{
  "scripts": {
    "start": "cross-env NODE_ENV=development node server/room-server.js",
    "build": "cd client && npm install && npm run build"
  },
  "dependencies": {
    "cross-env": "^7.0.3",
    "ws": "^7.4.3"
  }
}
